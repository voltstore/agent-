'use strict';
require('dotenv').config({ path: require('path').join(__dirname, '..', '.env') });
const https = require('https');
const { fbGet, fbSet, fbPush, logEvent, notifyOwner, checkBudget, DEFAULT_SETTINGS, MAX_FAILURES } = require('./utils');
const GOOGLE_API_KEY = process.env.GOOGLE_SEARCH_API_KEY;
const ALL_AREAS = ['الرياض','شمال الرياض','جنوب الرياض','شرق الرياض','غرب الرياض','العليا الرياض','الملقا الرياض','النرجس الرياض','الياسمين الرياض','الورود الرياض','الروضة الرياض','حي السليمانية الرياض','المحمدية الرياض','الخرج','الدرعية','الزلفي','المجمعة','شقراء','الدوادمي','الأفلاج','وادي الدواسر','السليل','جدة','شمال جدة','جنوب جدة','وسط جدة','الروضة جدة','الحمراء جدة','الزهراء جدة','البوادي جدة','الصفا جدة','الفيصلية جدة','السامر جدة','أبحر جدة','الشاطئ جدة','بريمان جدة','مكة المكرمة','العزيزية مكة','الشهداء مكة','الطائف','شمال الطائف','الهدا','الشفا','الكر','الجنوب الطائف','المدينة المنورة','العوالي المدينة','قباء','العقيق المدينة','الدمام','الخبر','الظهران','القطيف','سيهات','صفوى','العوامية','الجبيل','الأحساء','الهفوف','المبرز','العيون الأحساء','تبوك','شرما','البدع','حائل','بقعاء','الغزالة','عرعر','رفحاء','طريف','سكاكا','القريات','دومة الجندل','أبها','خميس مشيط','محايل عسير','النماص','بيشة','ظهران الجنوب','جازان','صبيا','أبو عريش','صامطة','الدرب','نجران','شرورة','حبونا','ينبع','رابغ','الليث','القنفذة','المويه','العرضيات'];
function placesSearch(query){return new Promise((resolve,reject)=>{const body={textQuery:query,languageCode:'ar',maxResultCount:20};const options={hostname:'places.googleapis.com',path:'/v1/places:searchText',method:'POST',headers:{'Content-Type':'application/json','X-Goog-Api-Key':GOOGLE_API_KEY,'X-Goog-FieldMask':'places.displayName,places.nationalPhoneNumber,places.internationalPhoneNumber,places.websiteUri,places.rating,places.userRatingCount,places.formattedAddress,places.id,nextPageToken'}};const req=https.request(options,(res)=>{let data='';res.on('data',chunk=>data+=chunk);res.on('end',()=>{try{resolve(JSON.parse(data))}catch(e){reject(e)}})});req.on('error',reject);req.write(JSON.stringify(body));req.end()})}
async function isAlreadySaved(phone,name){try{const leads=await fbGet('leads')||{};return Object.values(leads).some(l=>(phone&&l.phone===phone)||l.name===name)}catch{return false}}
function parsePlace(place,city,category){const name=place.displayName?.text||'';const phone=(place.nationalPhoneNumber||place.internationalPhoneNumber||'').replace(/\s|-/g,'');const hasWebsite=!!place.websiteUri;const rating=place.rating||0;const reviewCount=place.userRatingCount||0;const address=place.formattedAddress||city;return{name,phone,hasWebsite,rating,reviewCount,address,city,category}}
async function getNextArea(preferredCity){if(preferredCity)return preferredCity;try{const settings=await fbGet('settings')||{};const usedAreas=settings.usedAreas||[];const unused=ALL_AREAS.filter(a=>!usedAreas.includes(a));if(unused.length===0){await fbSet('settings/usedAreas',[]);return ALL_AREAS[Math.floor(Math.random()*ALL_AREAS.length)]}return unused[Math.floor(Math.random()*unused.length)]}catch{return ALL_AREAS[Math.floor(Math.random()*ALL_AREAS.length)]}}
async function markAreaUsed(area){try{const settings=await fbGet('settings')||{};const usedAreas=settings.usedAreas||[];if(!usedAreas.includes(area)){usedAreas.push(area);await fbSet('settings/usedAreas',usedAreas)}}catch{}}
async function runSora(options={}){const t0=Date.now();let failures=0,totalScanned=0,totalSkipped=0;try{if(!(await checkBudget()))return{success:false,reason:'ميزانية منتهية'};const settings=await fbGet('settings')||DEFAULT_SETTINGS;const cats=settings.categories||DEFAULT_SETTINGS.categories;const city=await getNextArea(options.city);const category=options.category||cats[Math.floor(Math.random()*cats.length)];const target=Number(options.target||settings.dailyTarget||30);const minRating=Number(settings.minRating||4.0);const minReviews=Number(settings.minReviews||5);await logEvent('info',`سورا: بدء البحث — ${city} | ${category} | الهدف: ${target}`);console.log(`\nسورا: ${city} | ${category} | الهدف: ${target}`);const found=[];let round=0;const maxRound=8;const queries=[`${category} ${city}`,`${category} في ${city}`,`أفضل ${category} ${city}`,`${category} ${city} خدمات`,`شركة ${category} ${city}`,`مؤسسة ${category} ${city}`,`${category} ${city} محترف`,`${category} بالقرب من ${city}`];while(found.length<target&&round<maxRound){round++;const query=queries[(round-1)%queries.length];console.log(`\n  ← جولة ${round}/${maxRound} — "${query}"`);console.log(`     متبقٍّ: ${target-found.length} | تم العثور: ${found.length}`);try{const results=await placesSearch(query);const places=results.places||[];totalScanned+=places.length;console.log(`     نتائج Google: ${places.length} مكان`);for(const place of places){if(found.length>=target)break;const company=parsePlace(place,city,category);if(company.hasWebsite){totalSkipped++;continue}if(company.rating<minRating){totalSkipped++;continue}if(company.reviewCount<minReviews){totalSkipped++;continue}if(!company.phone){totalSkipped++;continue}if(!company.name||company.name.length<3){totalSkipped++;continue}if(found.some(f=>f.phone===company.phone||f.name===company.name)){totalSkipped++;continue}if(await isAlreadySaved(company.phone,company.name)){console.log(`     ⟳ مكرر: ${company.name}`);totalSkipped++;continue}found.push(company);console.log(`  ✓ [${found.length}/${target}] ${company.name}`);console.log(`     📞 ${company.phone} | ⭐ ${company.rating} (${company.reviewCount} تقييم)`);await sleep(100)}failures=0;if(found.length<target&&round<maxRound){console.log(`     ⏳ انتظار 2 ثانية...`);await sleep(2000)}}catch(e){failures++;await logEvent('error',`سورا: خطأ جولة ${round}`,{error:e.message});if(failures>=MAX_FAILURES)throw e;await sleep(5000)}}await markAreaUsed(city);let saved=0;const now=new Date().toISOString();for(const c of found){try{await fbPush('leads',{...c,status:'جديد',discoveredAt:now,source:'Google Maps',contacted:false,notes:''});saved++}catch(e){await logEvent('error',`سورا: فشل حفظ ${c.name}`,{error:e.message})}}try{const today=new Date().toISOString().split('T')[0];const statsKey=`stats/daily/${today}`;const existing=await fbGet(statsKey)||{};await fbSet(statsKey,{...existing,soraFound:(existing.soraFound||0)+saved,soraScanned:(existing.soraScanned||0)+totalScanned,soraRuns:(existing.soraRuns||0)+1,lastRun:now})}catch(e){console.warn('تحذير:',e.message)}const secs=Math.round((Date.now()-t0)/1000);const mins=Math.round(secs/60);console.log(`\nسورا اكتمل! محفوظة: ${saved} | المنطقة: ${city} | المدة: ${mins>0?mins+' دقيقة':secs+' ثانية'}`);await logEvent('info',`سورا: اكتمل — ${saved} شركة من ${city}`);return{success:true,found:saved,scanned:totalScanned,skipped:totalSkipped,city,category}}catch(err){await logEvent('error','سورا: فشل عام',{error:err.message});return{success:false,error:err.message}}}
function sleep(ms){return new Promise(r=>setTimeout(r,ms))}
if(require.main===module){const[,,city,category,target]=process.argv;runSora({city,category,target:target?Number(target):undefined}).then(r=>{console.log('\nالنتيجة:',r);process.exit(0)}).catch(e=>{console.error(e);process.exit(1)})}
module.exports={runSora};


